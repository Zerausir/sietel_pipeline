# PostgreSQL analítico (VM1) -- rol de SOLO LECTURA sobre el esquema mart
MART_PG_HOST=192.168.129.50
MART_PG_PORT=5432
MART_PG_DATABASE=sietel_analitico
MART_PG_USER=dashboard_lector
MART_PG_PASSWORD=CAMBIE_ESTA_CONTRASENA

# PostgreSQL de autenticación (misma instancia VM1, esquema auth) -- rol
# separado, sin acceso a mart. Ver sql/03_ddl_auth.sql.
AUTH_PG_HOST=192.168.129.50
AUTH_PG_PORT=5432
AUTH_PG_DATABASE=sietel_analitico
AUTH_PG_USER=dashboard_auth
AUTH_PG_PASSWORD=CAMBIE_ESTA_CONTRASENA

# PostgreSQL de samm_pipeline (VM1, misma instancia física, base samm_db
# DISTINTA de sietel_analitico) -- módulo SMA. samm_user es, según lo que
# se pudo confirmar, el rol propietario completo de samm_pipeline, NO uno
# de solo lectura como dashboard_lector -- riesgo aceptado explícitamente
# por Iván el 26-ago-2026 (ver dashboard/services/database.py:get_sma_engine()
# para el detalle). Verificar nombres de columna reales antes de producción:
# ver advertencia en dashboard/services/queries_sma.py.
SMA_PG_HOST=192.168.129.50
SMA_PG_PORT=5432
SMA_PG_DATABASE=samm_db
SMA_PG_USER=samm_user
SMA_PG_PASSWORD=CAMBIE_ESTA_CONTRASENA

# Firma las cookies de sesión de Flask-Login. Generar una vez con:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CAMBIE_ESTO_GENERANDO_UNA_CLAVE_PROPIA

# Dash -- APP_DEBUG debe quedar en false en el contenedor de producción de VM2
APP_HOST=0.0.0.0
APP_PORT=8050
APP_DEBUG=false
CACHE_TIMEOUT=300
